define(["jquery"],function($){var defaults={edgeSize:80,maxSpeed:22},init=function(selector,edgeSize,maxSpeed){var options=$.extend({},defaults,{edgeSize:edgeSize||defaults.edgeSize,maxSpeed:maxSpeed||defaults.maxSpeed});$(selector).each(function(){var container=this,frame=null,active=false,pointerX=null,currentSpeed=0;function stop(){active=false,pointerX=null,currentSpeed=0,frame&&(window.cancelAnimationFrame(frame),frame=null)}function getSpeed(){if(!active||null===pointerX)return 0;var rect=container.getBoundingClientRect(),leftOffset=pointerX-rect.left,rightOffset=rect.right-pointerX;return container.scrollWidth<=container.clientWidth?0:leftOffset>=0&&leftOffset<options.edgeSize?-Math.ceil(options.maxSpeed*(1-leftOffset/options.edgeSize)):rightOffset>=0&&rightOffset<options.edgeSize?Math.ceil(options.maxSpeed*(1-rightOffset/options.edgeSize)):0}function tick(){if(!active)return void(frame=null);currentSpeed=getSpeed(),0===currentSpeed?(frame=window.requestAnimationFrame(tick)):((function(){var maxScrollLeft=container.scrollWidth-container.clientWidth,nextScrollLeft=container.scrollLeft+currentSpeed;nextScrollLeft<0?nextScrollLeft=0:nextScrollLeft>maxScrollLeft&&(nextScrollLeft=maxScrollLeft),container.scrollLeft=nextScrollLeft,frame=window.requestAnimationFrame(tick)})())}function start(event){active=true,pointerX=event.clientX,frame||(frame=window.requestAnimationFrame(tick))}function move(event){pointerX=event.clientX}container.addEventListener("mouseenter",start),container.addEventListener("mousemove",move),container.addEventListener("mouseleave",stop),container.addEventListener("wheel",stop,{passive:true})})};return{init:init}});
